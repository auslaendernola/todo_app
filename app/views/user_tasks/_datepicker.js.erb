$(document).ready(function () {
  $('#user_task_due').datepicker({
    autoclose: true,
    todayHighLight: true,
    toggleActive: true,
    format: "yyyy-mm-dd",
    orientation: "top-auto"
  });
});
